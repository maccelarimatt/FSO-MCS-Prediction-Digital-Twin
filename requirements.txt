# FSO-TVWS MCS Prediction - Phase 1
# Install: pip install -r requirements.txt

numpy>=1.24
scipy>=1.10
torch>=2.0
scikit-learn>=1.3
matplotlib>=3.7

# Phase-screen turbulence simulation (strongly recommended)
# Without this, the channel simulator falls back to a statistical
# model (AR(1) + Gamma-Gamma) which has correct marginal statistics
# but lacks the higher-order temporal structure of real turbulence.
# 
# Install separately if pip has issues:
#   pip install aotools
aotools>=1.0
